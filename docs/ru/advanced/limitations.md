# Ограничения и возможности вне области охвата

> nextorm — построитель запросов с небольшой явной поверхностью DML: он генерирует инструкции `SELECT` (и параметризованные `INSERT ... VALUES`, `INSERT ... SELECT`, key upsert, `MERGE`, `DELETE` и `UPDATE`) и материализует их результаты, и намеренно оставляет отслеживание изменений и `SaveChanges` вызывающей стороне.

**Предварительные требования:** [Provider overview](../providers/overview.md) · [API reference](api-reference.md)

## Обзор

На этой странице перечислены возможности, которые **не** являются частью nextorm, с однострочной причиной для каждой.

## Вне области охвата

| Не поддерживается | Обоснование |
|---|---|
| **DML** — `INSERT`, `UPDATE`, `DELETE`, `MERGE` | **`INSERT ... VALUES`, `INSERT ... SELECT` и key upsert реализованы** ([`InsertInto`](../guide/19-insert-statement.md); возврат строк через `RETURNING`/`OUTPUT` на PostgreSQL, SQLite и SQL Server, строки батча неупорядочены; серверный источник поднимает свои CTE перед `INSERT` и отклоняет рантайм-плейсхолдеры `SqlFunctions.Parameter`). PostgreSQL также поддерживает **модифицирующие CTE** (`With(имя, insert)` → [`MutationCteQuery<T>`](xref:NextORM.Core.MutationCteQuery`1), [модифицирующий CTE](../guide/19-insert-statement.md#модифицирующий-cte-postgresql)). **Upsert** ([`MergeInto`](xref:NextORM.Core.DataContextExtensions.MergeInto``1(NextORM.Core.IDataContext,System.Action{NextORM.Core.EntityMetadataBuilder{``0}})) → [`MergeBuilder<T>`](xref:NextORM.Core.MergeBuilder`1), [upsert](../guide/23-merge-statement.md#upsert-key-merge)) рендерит родную форму провайдера — `INSERT ... ON CONFLICT ... DO UPDATE` (PostgreSQL, SQLite), `INSERT ... ON DUPLICATE KEY UPDATE` (MySQL, MariaDB) или `MERGE ... USING (VALUES ...)` (SQL Server); ClickHouse их отклоняет, а провайдер in-memory применяет upsert к зарегистрированной последовательности. **`DELETE`** ([`DeleteFrom`](../guide/20-delete-statement.md) → [`DeleteBuilder<T>`](xref:NextORM.Core.DeleteBuilder`1), `Delete<T>(entity)`, [удаление](../guide/20-delete-statement.md)) рендерит параметризованный `DELETE FROM <table> [WHERE ...]` — по предикату, по объявленному ключу сущности или всю таблицу с явным `All()`; `Returning()` возвращает удалённые строки на PostgreSQL/SQLite (`RETURNING`) и SQL Server (`OUTPUT`), `Truncate<T>()` рендерит `TRUNCATE TABLE`, ClickHouse удаляет через `ALTER TABLE ... DELETE ... SETTINGS mutations_sync = 1`. Multi-table `DELETE` по INNER-соединению ([`From<T>().Join(...).Delete()`](../guide/20-delete-statement.md#удаление-по-соединению)) поддерживается на PostgreSQL, SQL Server, MySQL и MariaDB и отклоняется SQLite и ClickHouse. **`UPDATE`** ([`Update`](../guide/21-update-statement.md) → [`UpdateBuilder<T>`](xref:NextORM.Core.UpdateBuilder`1), `Update<T>(entity)`) рендерит параметризованный `UPDATE <table> SET ... [WHERE ...]` на PostgreSQL, SQL Server, SQLite, MySQL и MariaDB; ClickHouse обновляет через `ALTER TABLE ... UPDATE ... SETTINGS mutations_sync = 1` (синхронно, но число затронутых строк не возвращает), а провайдер in-memory его отклоняет; `Returning()` возвращает обновлённые строки на PostgreSQL/SQLite (`RETURNING`) и SQL Server (`OUTPUT`). Multi-table `UPDATE` по INNER-соединению ([`From<T>().Join(...).UpdateJoin()`](../guide/21-update-statement.md#обновление-из-join)) поддерживается на PostgreSQL/SQLite (`FROM`), SQL Server и MySQL/MariaDB и отклоняется ClickHouse. Полный `MERGE` с ветками `WHEN MATCHED`/`WHEN NOT MATCHED`/`WHEN NOT MATCHED BY SOURCE` и `MERGE ... RETURNING`/`OUTPUT` доступны на SQL Server и PostgreSQL 15+ и отклоняются остальными провайдерами. `INSERT` в провайдере in-memory — вне области по замыслу: этот контекст — поверхность запросов по коллекциям, зарегистрированным вызывающим кодом (`WithData`), а не эмуляция базы данных (в отличие от in-memory-провайдера EF Core, где `Add` + `SaveChanges` — часть модели БД), и ни один терминал записи (`INSERT`/`DELETE`/`TRUNCATE`) на нём недоступен, кроме key-upsert merge, применяемого к зарегистрированной последовательности. Отслеживания изменений и `SaveChanges` нет: запись — это явные команды. Запрос можно материализовать в постоянную таблицу через `CREATE TABLE ... AS SELECT` ([`ToTempTable`/`ToTable`](../guide/22-create-table-as.md)) на PostgreSQL, SQLite, MySQL, MariaDB, SQL Server и ClickHouse: SQL Server рендерит `SELECT ... INTO` (таблица уровня сессии — `ToTable("#name")`), ClickHouse — `CREATE TABLE ... ENGINE = MergeTree ... AS SELECT`. Временная форма `ToTempTable` доступна только на PostgreSQL, SQLite, MySQL и MariaDB: в SQL Server нет `CREATE TEMPORARY TABLE ... AS SELECT` (используйте `#name`), а ClickHouse не допускает временный `AS SELECT`. Утверждение с побочным эффектом и последующее чтение можно отправить одним батчем через общий `BatchBuilder` (материализации `CreateTempTable`/`CreateTable` и DML-шаги `Insert`/`Update`/`Delete`/`Truncate` перед одним читающим запросом), одним round trip на одной сессии, на PostgreSQL, SQL Server, MySQL, MariaDB и SQLite; ClickHouse и in-memory-контекст отклоняют батч с `NotSupportedException`, а DML-шаг не может запрашивать возврат строк (`Returning()` даёт другой тип билдера). SQL Server отправляет батч одной `;`-склеенной командой, а не `SqlBatch`, который выполняет каждую команду в своей области видимости, поэтому `#temp` остаётся видимой между утверждениями. Выполнение батча не проходит через интерцепторы запросов: их события несут `DbCommand`, тогда как команда `DbBatch` — это `DbBatchCommand`. |
| **`Prepare()` / кэш планов для DML** | Каждая мутация рендерит и выполняет одну команду за вызов — билдеры мутаций намеренно не готовятся и не кладутся в кэш планов. Оптимизация в nextorm нацелена только на read-only запросы (явный `Prepare`, неявный кэш планов, бенчмарки). |
| **Bulk-copy флаги вне SQL Server** | `BulkInsertOptions.CheckConstraints`/`TableLock`/`KeepNulls`/`FireTriggers` отображаются в `SqlBulkCopyOptions` на нативном пути `SqlBulkCopy` в SQL Server. Путь `COPY` в PostgreSQL и портируемый `INSERT ... VALUES` (MySQL/MariaDB/SQLite/ClickHouse, а также SQL Server, когда `Returning*` или `KeepIdentity` переключают запись на портируемый путь) отклоняют запрошенный флаг через `NotSupportedException`, а не игнорируют его. ClickHouse `MaxDegreeOfParallelism`/`WithoutSession` не выставляются: `ClickHouseBulkCopy` в `ClickHouse.Driver` 1.4.0 устарел, а не-устаревший `ClickHouseClient.InsertBinaryAsync` требует собственного клиента; сессия — настройка строки подключения (`UseSession`), а не пер-записная опция. `UseInternalTransaction` намеренно не выставляется (nextorm не открывает неявную транзакцию). См. [Массовая вставка](../guide/24-bulk-insert.md#опции-bulk-copy-в-sql-server). |
| **Навигационные свойства / метаданные связей** | Метаданных связей нет, и нет неявного вывода соединений; вы пишете соединения явно с помощью [`Join`](xref:NextORM.Core.EntityBuilder`1.Join``1(NextORM.Core.EntityBuilder{``0},System.Linq.Expressions.Expression{System.Func{`0,``0,System.Boolean}}))/[`LeftJoin`](xref:NextORM.Core.EntityBuilder`1.LeftJoin``1(NextORM.Core.EntityBuilder{``0},System.Linq.Expressions.Expression{System.Func{`0,``0,System.Boolean}}))/[`RightJoin`](xref:NextORM.Core.EntityBuilder`1.RightJoin``1(NextORM.Core.EntityBuilder{``0},System.Linq.Expressions.Expression{System.Func{`0,``0,System.Boolean}}))/[`FullJoin`](xref:NextORM.Core.EntityBuilder`1.FullJoin``1(NextORM.Core.EntityBuilder{``0},System.Linq.Expressions.Expression{System.Func{`0,``0,System.Boolean}}))/[`CrossJoin`](xref:NextORM.Core.EntityBuilder`1.CrossJoin``1(NextORM.Core.EntityBuilder{``0}))/[`CrossApply`](xref:NextORM.Core.EntityBuilder`1.CrossApply``1(NextORM.Core.EntityBuilder{``0}))/[`OuterApply`](xref:NextORM.Core.EntityBuilder`1.OuterApply``1(NextORM.Core.EntityBuilder{``0})) (см. [Соединения](../guide/03-joins.md)). |
| **Выбор соединённой сущности целиком** | После соединения накопленная проекция позиционна (`Item1`..`Item8`, по одному элементу на источник в порядке соединений), и выбирать можно только её колонки (`Select(p => p.Item1.Id)`); член проекции, являющийся сущностью целиком (`Select(p => p.Item1)`), колонкой не является и отклоняется. Придайте сторонам осмысленные имена, спроецировав в именованный тип (`Select(p => new { Order = p.Item1.Id, Customer = p.Item2.RequiredString })`); см. [Соединения](../guide/03-joins.md#адресация-проекции). |
| **Коррелированный источник `APPLY` / `LATERAL`** | [`CrossApply`](xref:NextORM.Core.EntityBuilder`1.CrossApply``1(NextORM.Core.EntityBuilder{``0}))/[`OuterApply`](xref:NextORM.Core.EntityBuilder`1.OuterApply``1(NextORM.Core.EntityBuilder{``0})) принимают лямбду, получающую строку левой стороны, поэтому применяемый источник может ссылаться на внешнюю строку у провайдера с lateral-источником (SQL Server `CROSS/OUTER APPLY`, PostgreSQL/MySQL/MariaDB `... LATERAL`). Диалекты без lateral-источника (SQLite, ClickHouse) отклоняют `APPLY`; провайдер in-memory его не поддерживает. Лямбда корреляции не может ссылаться на проекцию соединения — применяйте её к источнику из одной сущности. |
| **[`SelectMany`](xref:NextORM.Core.EntityBuilder`1.SelectMany``1(System.Linq.Expressions.Expression{System.Func{`0,System.Collections.Generic.IEnumerable{``0}}})) / [`GroupJoin`](xref:NextORM.Core.EntityBuilder`1.GroupJoin``3(NextORM.Core.EntityBuilder{``0},System.Linq.Expressions.Expression{System.Func{`0,``1}},System.Linq.Expressions.Expression{System.Func{``0,``1}},System.Linq.Expressions.Expression{System.Func{`0,System.Collections.Generic.IEnumerable{``0},``2}})) на SQL-провайдерах** | Эти LINQ-операторы реализованы только для провайдера in-memory, где корреляция — обычный делегат. На SQL-провайдере [`SelectMany`](xref:NextORM.Core.EntityBuilder`1.SelectMany``1(System.Linq.Expressions.Expression{System.Func{`0,System.Collections.Generic.IEnumerable{``0}}}))/[`GroupJoin`](xref:NextORM.Core.EntityBuilder`1.GroupJoin``3(NextORM.Core.EntityBuilder{``0},System.Linq.Expressions.Expression{System.Func{`0,``1}},System.Linq.Expressions.Expression{System.Func{``0,``1}},System.Linq.Expressions.Expression{System.Func{`0,System.Collections.Generic.IEnumerable{``0},``2}})) бросают `NotSupportedException`; явная поверхность соединений [`CrossApply`](xref:NextORM.Core.EntityBuilder`1.CrossApply``1(NextORM.Core.EntityBuilder{``0}))/[`OuterApply`](xref:NextORM.Core.EntityBuilder`1.OuterApply``1(NextORM.Core.EntityBuilder{``0})) покрывает некоррелированный эквивалент (см. [Соединения](../guide/03-joins.md)). |
| **Коррелированные подзапросы в провайдере in-memory глубже одного уровня** | Корреляция работает для скалярных подзапросов (включая агрегатные терминалы) и `EXISTS`/`IN` в `SELECT`, `WHERE` и `ORDER BY` у каждого SQL-провайдера на любой глубине вложенности, а у провайдера in-memory — для корреляции глубины один, с вычислением на каждую внешнюю строку (см. [Подзапросы](../guide/06-subqueries.md#коррелированный-скалярный-подзапрос)). Оставшиеся формы in-memory отклоняются с `NotSupportedException`: глубина корреляции больше одной, ссылка на внешнюю строку во внутренней проекции или `ORDER BY`, коррелированный `GROUP BY`/`HAVING`, асинхронный источник и смешение внешних ссылок с явными `SqlFunctions.Parameter`-плейсхолдерами. |
| **Коррелированный скаляр: терминалы `First`/`Single` и их `*OrDefault`-формы** | При nullable-проекции отсутствие строки даёт `null`, при non-nullable value-проекции терминалы `FirstOrDefault`/`SingleOrDefault` дают `default` (например `0`). `First`/`Single` на non-nullable value-проекции бросают исключение, если строки нет. `Single`/`SingleOrDefault` при более чем одной строке бросают у всех провайдеров, чьи скалярные подзапросы проверяют кардинальность (PostgreSQL, SQL Server, MySQL, MariaDB, ClickHouse); SQLite её не проверяет, поэтому там числовой `Single`/`SingleOrDefault` при более чем одной строке поднимает ошибку БД через рендер-гард `case when count(*) > 1 then <числовое переполнение> else value end`, а нечисловая проекция отклоняется с `NotSupportedException`. |
| **`INTERSECT ALL` / `EXCEPT ALL` в SQL Server и SQLite** | Ни один из этих движков не может выразить такие варианты, поэтому диалект отклоняет их с `NotSupportedException`; `INTERSECT` / `EXCEPT` (без `ALL`) и `UNION` / `UNION ALL` работают везде. PostgreSQL поддерживает оба варианта `ALL`. |
| **Блокирующие табличные хинты вне SQL Server** | Блокирующие табличные хинты (`WithTableHint("nolock")` → `WITH (NOLOCK)`) поддерживаются только в SQL Server; нативные index-хинты MySQL/MariaDB (`USE`/`FORCE`/`IGNORE INDEX`), SQLite (`INDEXED BY`/`NOT INDEXED`) и SQL Server (`WITH (INDEX(...))`) — отдельная поверхность ([`WithIndex`](../guide/17-query-hints.md#index-хинты), [`ISqlDialect.IndexHints`](xref:NextORM.Core.ISqlDialect.IndexHints)). Хинты уровня инструкции подключены в SQL Server (`OPTION (...)`), PostgreSQL и MySQL/MariaDB (`/*+ ... */`); SQLite и ClickHouse отклоняют `Hint(...)` через `NotSupportedException` (см. [Хинты запросов](../guide/17-query-hints.md)). |
| **ВЕРХНИЙ регистр SQL-ключевых слов — конструкторы row/tuple остаются каноничными для провайдера** | `DataContextBuilder.UseKeywordCase(KeywordCase.Upper)`, `EntityBuilder<TEntity>.WithKeywordCase(...)` и `QueryCommand<T>.WithKeywordCase(...)` поднимают регистр ключевых слов, которые эмитит ядро сборки инструкции (`select`, `from`, `where`, `join`, `group by`, `order by`, `with`, `insert`, `values`, ...), трансляторы выражений (`and`/`or`/`not`, `case`/`when`/`then`/`else`/`end`, `cast(... as ...)`, `is [not] null`, `like`, `in`, `exists`, `over` и оконные фреймы), общие диалектные клозы (алиасы `as`, пагинация `limit`/`offset`/`fetch`/`top`, `with [recursive]`, ключевые слова join, table/index-хинты) и **все диалектные клозы**: `for json`/`for xml`, модификаторы группировки (`rollup`/`cube`/`grouping sets`/`with totals`), `for system_time ...`, `apply`, `settings`, `final`, `sample`, DML `returning`/`output`, query-hint `option (...)`/`option (maxrecursion ...)` и capability-клозы (`for update`/`for share`, `tablesample ... repeatable`, `limit by`, `array join`, `distinct on`, `pivot`/`unpivot`). Конструкторы row/tuple `ROW(...)` (PostgreSQL) и `tuple(...)` (ClickHouse) трактуются как функциональные конструкторы и сохраняют каноничное написание провайдера в обоих режимах (согласовано с политикой имён функций). Идентификаторы, строковые литералы, имена функций, имена типов и raw SQL не меняются никогда, а `KeywordCase.Lower` (по умолчанию) побайтово совпадает с прежним выводом. |
| **Функции массивов вне PostgreSQL** | Квантификаторы `any`/`all` над параметром-массивом (`column = any(@array)`) и функции массивов (`cardinality`, `array_length`, ...) требуют провайдера с нативными массивами; его включает только PostgreSQL ([`SupportsArrays`](xref:NextORM.Core.ISqlDialect.SupportsArrays)). Остальные диалекты отклоняют их через `NotSupportedException` (см. [Скалярные функции](../scalar-functions/06-arrays.md#массивы-postgresql)). |
| **Поэлементный доступ к серверному массиву/JSON (`x[i]`)** | Поддерживается только захваченная (замкнутая) коллекция, индексируемая внутри запроса ([lookup по захваченной коллекции](../guide/02-filtering-where.md#lookup-по-захваченной-коллекции-dictcolumn)): ключ-константа сворачивается в параметр, ключ-колонка даёт переносимый `CASE`. Поэлементный доступ к серверной array/JSON-колонке или к array-returning выражению (`groupArray(x)[0]`, `array_agg(x)[1]`, `json['k']`) не имеет переносимой SQL-формы и бросает `NotSupportedException` (кастомный индексер — тоже) вместо привязки всей коллекции. Нативные формы различаются (`arr[i]` в PostgreSQL, `arrayElement(arr, i)` в ClickHouse, JSON-пути в остальных), поэтому нужен отдельный dialect-hook. |
| **`string.Split` вне ClickHouse** | CLR-метод `string.Split` рендерится как `splitByChar(separator, value)` только в ClickHouse ([`StringSplit`](xref:NextORM.Core.ISqlDialect.StringSplit)); PostgreSQL покрывает его своим array-операндным путём `string_to_array`, а SQL Server (`STRING_SPLIT` — табличная функция), MySQL/MariaDB и SQLite не имеют скалярного разбиения на массив и отклоняют вызов. Поддерживается только одноразрядный разделитель (без `splitByString`), а результат — `string[]`, который можно проецировать напрямую или использовать внутри другой array-функции. |
| **Нативные JSON-функции вне PostgreSQL** | Функции и операторы `json`/`jsonb` (`json_agg`, `json_build_object`, `->`, `->>`, `#>`, `@>`, `?`, ...) требуют провайдера с типом JSON; его включает только PostgreSQL ([`SupportsJson`](xref:NextORM.Core.ISqlDialect.SupportsJson)). В SQL Server, MySQL и MariaDB вместо этого есть текстовое подмножество JSON (`json_value`/`json_query`/`json_modify`/`isjson`, [`SupportsTextJson`](xref:NextORM.Core.ISqlDialect.SupportsTextJson)) над обычной текстовой колонкой (MySQL/MariaDB рендерят его через `JSON_EXTRACT`/`JSON_SET`). Остальные диалекты отклоняют обе поверхности через `NotSupportedException` (см. [Скалярные функции](../scalar-functions/07-json-and-xml.md#json-и-jsonb-postgresql)). |
| **Нативный тип колонки `JSON` в ClickHouse** | Функции нативного JSON (`JSONAllPaths`/`JSONAllPathsWithTypes`/`toJSONString`, наряду с `JSON_VALUE`/`JSON_QUERY`/`JSON_EXISTS`) подключены на `ClickHouseFunctions` и включаются флагом [`SupportsJsonExtract`](xref:NextORM.Core.ISqlDialect.SupportsJsonExtract), но нативная *колонка* `JSON` не отображается на CLR-тип: `ClickHouse.Driver` читает её как `System.Text.Json.Nodes.JsonObject`, который row reader материализовать не умеет. Храните JSON в колонке `String` (или приведите колонку в SQL), если её нужно материализовать; отдельный type-mapping/reader — отдельный пункт бэклога. |
| **`greatest`/`least` у провайдеров без поддержки** | Функции включаются флагом [`SupportsGreatestLeast`](xref:NextORM.Core.ISqlDialect.SupportsGreatestLeast); PostgreSQL, MySQL/MariaDB, ClickHouse, SQL Server 2022+ и SQLite его включают. SQLite рендерит многоаргументные скалярные `max`/`min` (один аргумент возвращается как есть, так как `max`/`min` с одним аргументом — агрегат). Семантика NULL различается: PostgreSQL, SQL Server 2022+ и ClickHouse 24.12+ игнорируют NULL-аргументы, а MySQL/MariaDB и SQLite возвращают NULL, если любой аргумент NULL. Провайдер, не включивший флаг, отклоняет вызовы через `NotSupportedException`. `nullif` — ANSI и доступен всегда. |
| **Кросс-провайдерные скалярные функции у провайдеров без функции** | `left`/`right`/`lpad`/`rpad`/`repeat`/`reverse`/`space`/`concat_ws`/`translate`/`ascii`/`@char` гейтятся **по имени** через [`IScalarFunctions.Supports`](xref:NextORM.Core.IScalarFunctions.Supports(System.String)). В MySQL/MariaDB нет `translate`; в SQLite нет `lpad`/`rpad`/`repeat`/`reverse`/`space`/`translate`, поэтому там они отклоняются с `NotSupportedException`. `left`/`right`/`lpad`/`rpad` считают символы (в ClickHouse используются варианты `*UTF8`); `concat_ws` пропускает NULL-аргументы везде, кроме ClickHouse, чей `concatWithSeparator` возвращает NULL. `ascii`/`char` следуют нативным правилам провайдера для не-ASCII ввода (в ClickHouse `char(n)` — байтовая строка), а отрицательный `n` у `left`/`right` зависит от провайдера. In-memory провайдер вычисляет вызовы через их CLR-эквиваленты и читает отрицательный `n` как PostgreSQL («всё кроме последних/первых |n|»), поэтому для переносимых запросов используйте неотрицательный `n`. См. [Скалярные функции](../scalar-functions/01-string-functions.md#кросс-провайдерные-скалярные-функции). |
| **`date_trunc` вне PostgreSQL, SQL Server и ClickHouse** | `date_trunc` включается флагом [`SupportsDateTrunc`](xref:NextORM.Core.ISqlDialect.SupportsDateTrunc); его включают PostgreSQL, SQL Server 2022+ (`datetrunc`) и ClickHouse (`dateTrunc`). |
| **Функции приведения/частей даты ClickHouse вне ClickHouse** | Поверхность `toDate`/`toDateTime`/`toDate32`, `toYear`/…, `toStartOf*`, `toMonday`, `toYYYYMM`/`toYYYYMMDD` и `toUnixTimestamp` ([`DateConversion`](xref:NextORM.Core.ISqlDialect.DateConversion)) доступна только в ClickHouse; остальные провайдеры отклоняют её через `NotSupportedException`. Портируемые аналоги — `date_trunc`/`date_add`/`end_of_month` и части `DateTime` (см. [Скалярные функции](../scalar-functions/03-date-and-time.md#приведение-и-части-даты-clickhouse)). |
| **ClickHouse `multiIf` и `lagInFrame`/`leadInFrame` вне ClickHouse** | Многоветвевный `multi_if` ([`MultiIf`](xref:NextORM.Core.ISqlDialect.MultiIf)) и учитывающие фрейм оконные `lag_in_frame`/`lead_in_frame` ([`SupportsInFrameWindowFunctions`](xref:NextORM.Core.ISqlDialect.SupportsInFrameWindowFunctions)) доступны только в ClickHouse; остальные провайдеры отклоняют их через `NotSupportedException`. Многоветвевные условия портируемы через `iif`/C#-тернарник (рендерит `CASE WHEN`), а стандартные `lag`/`lead` — замена на основе партишена; но сам ClickHouse отвергает явный фрейм у них, поэтому frame-aware сосед требует `lagInFrame`/`leadInFrame`. |
| **`array_agg` вне PostgreSQL** | `array_agg` включается флагом [`SupportsArrayAgg`](xref:NextORM.Core.ISqlDialect.SupportsArrayAgg) и требует типа-массива на стороне провайдера, поэтому его включает только PostgreSQL. `string_agg` включается отдельным флагом [`SupportsStringAgg`](xref:NextORM.Core.ISqlDialect.SupportsStringAgg) и доступен также в SQL Server 2017+ и ClickHouse (`arrayStringConcat(groupArray(x), delimiter)`). Результат `array_agg` — колонка `Array(T)`, которую row reader материализует как `T[]`, поэтому её можно проецировать напрямую, а также использовать внутри запроса (например, в `HAVING`). |
| **`untuple` (ClickHouse)** | `untuple(tuple)` не отображается: он разворачивает кортеж в несколько колонок результата, и у такого выражения нет единой скалярной CLR-формы в проекции. Проецируйте `Tuple(...)` как `System.Tuple<...>` и читайте элементы через `tupleElement`/`System.Tuple<...>.ItemN`. |
| **Материализация row-значений PostgreSQL, tuple `IN` и конструктор `(a, b)` вне PostgreSQL** | Row-значения конструируются и сравниваются в PostgreSQL (`ROW(a, b)`, элементы через `(row).fN`) и ClickHouse (`tuple(a, b)`, `tupleElement`); `.ItemN` inline-конструктора сворачивается в аргумент везде. Пока открыто: материализация «сырого» `ROW(...)` обратно в `System.Tuple<...>` в PostgreSQL (Npgsql требует `EnableRecords()`/`MapComposite`), tuple `IN`/`Contains` по списку CLR-кортежей и конструктор `(a, b)` в MySQL/MariaDB/SQLite (фаза 2). См. [Скалярные функции](../scalar-functions/index.md). |
| **Привязка сразу нескольких элементов `ARRAY JOIN` (ClickHouse)** | [`ArrayJoinElement`](xref:NextORM.Core.EntityBuilder`1.ArrayJoinElement``1(System.Linq.Expressions.Expression{System.Func{`0,System.Collections.Generic.IEnumerable{``0}}}))/[`LeftArrayJoinElement`](xref:NextORM.Core.EntityBuilder`1.LeftArrayJoinElement``1(System.Linq.Expressions.Expression{System.Func{`0,System.Collections.Generic.IEnumerable{``0}}})) привязывают элемент ровно одного array-выражения к `p.Element`; повторный вызов, вызов на соединённом запросе или вызов после `Where`/`Having` бросают `InvalidOperationException`. Когда вы делаете array join нескольких массивов и типизированный элемент не нужен, используйте [`ArrayJoin`](xref:NextORM.Core.EntityBuilder`1.ArrayJoin``1(System.Linq.Expressions.Expression{System.Func{`0,``0}}))/[`LeftArrayJoin`](xref:NextORM.Core.EntityBuilder`1.LeftArrayJoin``1(System.Linq.Expressions.Expression{System.Func{`0,``0}})), принимающие несколько выражений (см. [провайдер ClickHouse](../providers/clickhouse.md)). |
| **Состояние агрегатных функций ClickHouse (`-State`/`-Merge`, `runningAccumulate`)** | `uniqState`/`sumState`/`countState`/… возвращают opaque-значение `AggregateFunction(...)`, которое nextorm не моделирует: `ClickHouse.Driver` 1.4.0 бросает `AggregateFunctionException` из `FrameworkType`/`Read`/`Write`, поэтому такую колонку нельзя материализовать в CLR-значение или передать как него (сообщение драйвера указывает на парный `-Merge`). Сам `uniqMerge(uniqState(x))` запрещён (`ILLEGAL_AGGREGATION`), поэтому `-Merge` требует состояния, полученного на сервере (подзапрос или таблица-состояние), а `runningAccumulate` в ClickHouse 25.8 объявлен устаревшим (`DEPRECATED_FUNCTION`, нужен `allow_deprecated_error_prone_window_functions`). |
| **Логические и регрессионные агрегаты вне PostgreSQL** | `bool_and`/`bool_or`/`every` ([`SupportsBooleanAggregates`](xref:NextORM.Core.ISqlDialect.SupportsBooleanAggregates)) и семейство `regr_*` ([`SupportsRegressionAggregates`](xref:NextORM.Core.ISqlDialect.SupportsRegressionAggregates)) доступны только в PostgreSQL. `corr`/`covar_*` ([`SupportsStatisticalAggregates`](xref:NextORM.Core.ISqlDialect.SupportsStatisticalAggregates)) доступны также в ClickHouse (`corr`/`covarPop`/`covarSamp`). |
| **`FILTER` у агрегатов вне PostgreSQL, SQLite и ClickHouse** | Фильтрованный агрегат рендерится согласно [`AggregateFilterStyle`](xref:NextORM.Core.ISqlDialect.AggregateFilterStyle): PostgreSQL и SQLite генерируют ANSI-предложение `FILTER (WHERE ...)`, ClickHouse — комбинатор `-If` (`countIf`/`sumIf`/...), а MySQL/MariaDB, SQL Server и провайдер in-memory отклоняют его через `NotSupportedException`. |
| **`GROUP BY CUBE`/`GROUPING SETS` в MySQL/MariaDB и в провайдере in-memory** | В MySQL/MariaDB нет `CUBE` и `GROUPING SETS`, а провайдер in-memory не поддерживает ни `ROLLUP`, ни `CUBE`, ни `GROUPING SETS`; они отклоняют модификатор через `NotSupportedException`. `ROLLUP` работает на всех SQL-провайдерах (см. [Группировка и агрегаты](../guide/04-grouping-and-aggregates.md#rollup-и-cube)). |
| **Форматирование строк и коллация за пределами документированного подмножества** | `string.Format`/интерполяция/`value.ToString(format)` транслируют только culture-invariant подмножество: числа `N`/`F`/`D`/`X` (зависит от провайдера) и токены даты `yyyy yy MM dd HH mm ss` с разделителями `- / . : T` и пробелом. Любой другой спецификатор, как и любая культура, кроме `CultureInfo.InvariantCulture`, бросает `NotSupportedException`. Ordinal-перегрузки `StringComparison` принимают только `Ordinal`/`OrdinalIgnoreCase` (`InvariantCulture`/`CurrentCulture` бросают); SQLite дополнительно отклоняет регистрозависимые ordinal `Contains`/`StartsWith`/`EndsWith`, потому что его `LIKE` всегда регистронезависим независимо от коллации ([`SupportsOrdinalLike`](xref:NextORM.Core.ISqlDialect.SupportsOrdinalLike)). См. [Ordinal-сравнение и коллация](../scalar-functions/01-string-functions.md#ordinal-сравнение-и-коллация). |
| **Коллация на уровне столбца на ClickHouse и DDL** | Коллация, объявленная на свойстве сущности ([`CollationAttribute`](xref:NextORM.Core.CollationAttribute) / [`EntityPropertyBuilder<T>.Collation`](xref:NextORM.Core.EntityPropertyBuilder`1.Collation(System.String))), применяется к столбцу в collation-чувствительных операциях запроса на провайдерах с коллацией на уровне выражения ([`SupportsCollation`](xref:NextORM.Core.ISqlDialect.SupportsCollation)): PostgreSQL, SQL Server, MySQL/MariaDB и SQLite. У ClickHouse клаузы `COLLATE` нет — колонка с объявленной коллацией бросает `NotSupportedException`. nextorm не генерирует DDL, поэтому объявление описывает коллацию **существующего** столбца и не создаёт её. См. [Коллация на уровне столбца](../scalar-functions/01-string-functions.md#коллация-на-уровне-столбца). |
| **Регулярные выражения** | `Regex.IsMatch`/`Regex.Replace` с константным шаблоном транслируются на PostgreSQL, MySQL/MariaDB, ClickHouse, SQLite и SQL Server 2025+ ([`SupportsRegex`](xref:NextORM.Core.ISqlDialect.SupportsRegex)); в SQL Server совпадение рендерится как `REGEXP_LIKE`, которому нужен уровень совместимости БД 170, а `REGEXP_REPLACE` доступна на любом уровне — в SQL Server 2022 и раньше функций `REGEXP_*` нет. Шаблон, замена и опции должны быть константами времени компиляции, отображается только `RegexOptions.IgnoreCase`, а шаблон исполняет собственный движок провайдера (POSIX/ICU/PCRE/RE2/.NET), поэтому сложные шаблоны (lookaround, обратные ссылки) и ссылки на группы в замене непереносимы. См. [Регулярные выражения](../scalar-functions/01-string-functions.md#регулярные-выражения). |
| **Хранимые процедуры и динамический SQL** | nextorm строит `SELECT`-запросы из LINQ; вызов хранимой процедуры или сборка SQL во время выполнения — вне поверхности билдера (сырой SQL для запроса целиком доступен — [`WithSql`](xref:NextORM.Core.EntityExtensions.WithSql``1(NextORM.Core.EntityBuilder{``0},System.String))). |
| **Несколько result-set'ов из одной команды** | Батч выполняет утверждения с побочным эффектом, а затем единственный читающий запрос; исполнитель пропускает пустые наборы этих утверждений (`NextResult`), но публичного терминала, материализующего несколько наборов по порядку, нет. Чтение всех наборов, возвращаемых хранимой процедурой, пока не доступно (отложено). |
| **`OUTPUT ... INTO` вне SQL Server** | Только SQL Server умеет записывать изменённые строки в существующую таблицу через `OUTPUT ... INTO` ([`SupportsOutputInto`](xref:NextORM.Core.ISqlDialect.SupportsOutputInto)); остальные провайдеры отклоняют `OutputInto`/`OutputIntoThenOutput` с `NotSupportedException`. nextorm не объявляет табличную переменную (`DECLARE @t TABLE ...`) — цель это имя существующей таблицы (отложено). |
| **Внешние источники (`OPENROWSET`/`OPENQUERY`, linked servers)** | Вне области охвата; запрашивается схема самого соединения. |
| **Табличные функции ClickHouse `format`/`merge`/`input`** | Не предобъявлены на `SqlFunctions.ClickHouse`: `format` и `merge` выводят схему в рантайме, а `input` допустима только в `INSERT ... SELECT`, поэтому ни у одной нет схемы `TRow`, объявляемой вызывающим. Используйте `FromSql` или пользовательскую обёртку `[SqlTableFunction]`. Свободные ClickHouse `values` и PostgreSQL `jsonb_to_record`/`jsonb_to_recordset` **доступны** через объявляемую вызывающим схему `TRow` ([Динамическая схема результата](../guide/13-table-valued-functions.md#dynamic-result-schema)). |
| **PostgreSQL `json_populate_record(set)`** | Варианты `json_populate_record`/`json_populate_recordset` заполняют переданный вызывающим **базовый тип записи**, а не свободный список колонок, поэтому им нужен mapped-композит на провайдер; доступны только свободные `jsonb_to_record`/`jsonb_to_recordset`. См. [Динамическая схема записи](../guide/provider-specific/postgresql.md#динамическая-схема-записи). |
| **DDL / административные команды** | `CREATE`/`ALTER`/`DROP`, `OPTIMIZE`, `RENAME` и подобные не генерируются; используйте свои команды. |
| **Единица работы, `SaveChanges` и отслеживание изменений / CDC** | nextorm не владеет единицей работы и не отслеживает сущности, а `SaveChanges` нет. При этом запросы можно выполнять внутри транзакции: начать её через [`ITransactionManager`](xref:NextORM.Core.ITransactionManager) или встроиться в чужую (EF Core, Dapper, сырой ADO.NET) через `UseTransaction`. Запись остаётся явными командами `INSERT`/`UPDATE`/`DELETE`/`MERGE`; конкурентность проверяется явно (токен в `WHERE` + число затронутых строк), а слой отслеживания изменений можно построить сверху — см. [Оптимистичная конкурентность и отслеживание изменений](../guide/29-optimistic-concurrency.md). В ClickHouse транзакций нет, он их отклоняет. |
| **Режимы ожидания блокировки строк (`NOWAIT`/`SKIP LOCKED`) вне PostgreSQL, MySQL, MariaDB и SQL Server** | [`LockWaitMode`](xref:NextORM.Core.LockWaitMode) выражают четыре диалекта, умеющие блокировать строки ([`ILockRenderer`](xref:NextORM.Core.ILockRenderer)): PostgreSQL рендерит `FOR UPDATE`/`FOR SHARE [NOWAIT \| SKIP LOCKED]` (9.5+); MySQL 8.0.1+ рендерит `FOR UPDATE`/`FOR SHARE [NOWAIT \| SKIP LOCKED]`, причём разделяемая блокировка с режимом переключается с `LOCK IN SHARE MODE` (не принимающего lock-option) на `FOR SHARE`; MariaDB дописывает режим к `FOR UPDATE`/`LOCK IN SHARE MODE` (`NOWAIT` 10.3+, `SKIP LOCKED` 10.6+); SQL Server добавляет `NOWAIT`/`READPAST` в тот же табличный хинт, где `READPAST` пропускает любую заблокированную строку и потому приближает `SKIP LOCKED`. Пороги версий документируются, а не гейтятся по версии сервера. SQLite, ClickHouse и in-memory провайдер отклоняют любую блокировку строк — с режимом или без. |
| **Таймаут команды на ClickHouse** | `UseCommandTimeout`/`WithCommandTimeout` выставляют ADO.NET-свойство `DbCommand.CommandTimeout` на всех SQL-провайдерах, но `ClickHouse.Driver` 1.4.0 не отображает его на HTTP-запрос — запрос ограничивает `Timeout` соединения (по умолчанию две минуты), — поэтому там значение не действует. In-memory-провайдер его игнорирует (команды нет). Per-query-переопределение действует для запросов на чтение; DML-команды и батчи используют контекстный дефолт. |
| **Интерцепторы за пределами жизненного цикла команды/соединения** | Поставленный интерфейс — [`IQueryInterceptor`](xref:NextORM.Core.IQueryInterceptor) и [`IConnectionInterceptor`](xref:NextORM.Core.IConnectionInterceptor) (см. [Интерцепторы](../guide/27-interceptors.md)). Exception-интерцептора нет — транслируйте исключение провайдера через `try`/`catch` вокруг вызова терминала; интерцепторов для in-memory провайдера нет, у него нет команды и соединения. Интерцептор не должен менять `CommandText` (команда принадлежит кэшированному плану). Глобальные фильтры запросов — отдельный слой. |

| **Нативные range/multirange-колонки вне PostgreSQL** | Нативные range-типы (`int4range`…`daterange`) и multirange-типы (`int4multirange`…`datemultirange`) есть только в PostgreSQL и гейтятся [`SupportsRanges`](xref:NextORM.Core.ISqlDialect.SupportsRanges). В остальных провайдерах «голая» колонка `Range<T>`/`Range<T>[]` отклоняется с `NotSupportedException`. Вместо неё свойство `Range<T>` можно хранить парой скалярных колонок через [`RangeColumnsAttribute`](xref:NextORM.Core.RangeColumnsAttribute) (или флюентно `EntityPropertyBuilder<T>.RangeColumns(...)`, гейт [`SupportsRangeColumns`](xref:NextORM.Core.ISqlDialect.SupportsRangeColumns)): обе границы читаются и пишутся, <c>NULL</c> означает неограниченную сторону, а предикаты/инспекция (`overlaps`, `range_contains`/`range_contained_by`, позиционные и adjacency-предикаты, `lower`/`upper`/`isempty`/…) транслируются поверх пары. Range-возвращающие операторы (`range_union`/`range_intersection`/`range_difference`, конструкторы) и агрегаты `range_agg`/`range_intersect_agg` не имеют скалярной формы над парой и остаются только в PostgreSQL, как и multirange-колонки; in-memory провайдер вычисляет всю поверхность над CLR-значениями [`Range<T>`](xref:NextORM.Core.Range`1)/`Range<T>[]`. |

## Не ограничения

Следующее полностью реализовано и проверено; оно появляется в столбце *готово* анализа пробелов,
а не в списке выше:

- соединения любого типа (`INNER`, `LEFT`, `RIGHT`, `FULL`, `CROSS`, а также `CROSS APPLY`/`OUTER APPLY` и lateral-эквивалент там, где поддерживается) и арности 2–8, включая производный запрос с любой стороны (первичный источник `FROM` или присоединяемая таблица);
- коррелированные скалярные подзапросы и коррелированные `EXISTS`/`IN`/`ANY`/`ALL` в `SELECT`, `WHERE`, `ORDER BY` и `HAVING` у каждого SQL-провайдера на любой глубине вложенности, а также коррелированный скаляр/`EXISTS`/`IN` глубины один в `SELECT`/`WHERE`/`ORDER BY` у провайдера in-memory (см. [Подзапросы](../guide/06-subqueries.md));
- `CASE WHEN` / тернарный оператор / `switch`, `COALESCE`, числовой `CAST`;
- строковые, математические и date/time скалярные функции и `LIKE`;
- кросс-провайдерные скалярные строковые/числовые функции ([`SqlFunctions.Sql`](xref:NextORM.Core.SqlFunctions.Sql) `left`/`right`/`lpad`/`rpad`/`repeat`/`reverse`/`space`/`concat_ws`/`translate`/`ascii`/`@char`) с описанными выше гейтами по имени; in-memory провайдер вычисляет их через CLR-эквиваленты;
- арифметика дат (`date_add`/`end_of_month`/`date_diff`/`date_from_parts` и `DateTime.Add*`) в PostgreSQL, SQL Server, ClickHouse, MySQL/MariaDB и SQLite, а также `date_trunc` в PostgreSQL/SQL Server/ClickHouse и `string_agg` в PostgreSQL/SQL Server/ClickHouse/MySQL/MariaDB/SQLite;
- битовые (`bit_and`/`bit_or`/`bit_xor`), статистические (`corr`/`covar_*`) и `argMin`/`argMax` агрегаты в ClickHouse;
- бинарные столбцы — свойство или проекция `byte[]` отображается на `bytea` (PostgreSQL), `varbinary`/`image` (SQL Server) или `blob` (SQLite);
- 64-битные беззнаковые целые — свойство или проекция `ulong`/`ulong?` материализуется через аксессор построителя строк `DbDataReader.GetFieldValue<ulong>` у провайдеров с нативным беззнаковым типом (ClickHouse `UInt64`, MySQL/MariaDB `BIGINT UNSIGNED`), без SQL-приведения; в PostgreSQL, SQL Server и SQLite беззнакового целого нет, и этот диапазон выражается только `numeric`/`decimal`;
- `IN` по списку/массиву, логические `!` и унарные операторы;
- `SELECT DISTINCT`, `INTERSECT`/`EXCEPT`, CTE (включая рекурсивные) и оконные функции;
- модифицирующие CTE (`WITH n AS (INSERT ... RETURNING ...)`) на PostgreSQL, читаются типизированно через [`MutationCteQuery<T>`](xref:NextORM.Core.MutationCteQuery`1) (см. [модифицирующий CTE](../guide/19-insert-statement.md#модифицирующий-cte-postgresql));
- `OUTPUT ... INTO <target>` на SQL Server — `Returning(...).OutputInto(target)` записывает изменённые строки в существующую таблицу, `Returning(...).OutputIntoThenOutput(target)` записывает их и возвращает клиенту (см. [гайд по вставке](../guide/19-insert-statement.md#запись-изменённых-строк-в-таблицу-output-into));
- key-upsert `Returning()` — PostgreSQL/SQLite добавляют `RETURNING` к `ON CONFLICT ... DO UPDATE`, SQL Server рендерит `MERGE ... OUTPUT`; MySQL/MariaDB отклоняют его с `NotSupportedException` (см. [upsert](../guide/23-merge-statement.md#возврат-слитых-строк));
- пользовательские скалярные функции (`[SqlFunction]`) и табличные функции (`[SqlTableFunction]`);
- серверные/кластерные табличные функции ClickHouse `url`/`s3`/`file`/`remote`/`remoteSecure`/`cluster`/`clusterAllReplicas`, предобъявленные на `SqlFunctions.ClickHouse.*` с generic-интерфейсом строки `TRow`, объявляемым вызывающим (гейт — [`SupportsTableFunction`](xref:NextORM.Core.ISqlDialect.SupportsTableFunction(System.String)); см. [Табличные функции](../guide/13-table-valued-functions.md#встроенные-табличные-функции));
- предикаты полнотекстового поиска (`SqlFunctions.Sql.contains`/`freetext`) в SQL Server, PostgreSQL и MySQL/MariaDB, полнотекстовое ранжирование (SQL Server `containstable`/`freetexttable` с `KEY`/`RANK`, PostgreSQL `ts_rank`/`ts_rank_cd`), текстовые JSON-функции (`json_value`/`json_query`/`json_modify`/`isjson`) в SQL Server и MySQL/MariaDB и табличные функции SQL Server `openjson`/`string_split` (а также пользовательские обёртки `[SqlTableFunction]`, включая MySQL `JSON_TABLE` через `CallClause`);
- хинты уровня инструкции в SQL Server (`OPTION (...)`), PostgreSQL и MySQL/MariaDB (`/*+ ... */`), табличные хинты в SQL Server (`Hint(...)`, `WithTableHint(...)`), JSON-вывод (`ForJson(...)` → `FOR JSON PATH`/`AUTO`) и XML-вывод (`ForXml(...)` → `FOR XML`);
- методы типа XML в SQL Server (`xml_value`/`xml_query`/`xml_exist` → `xml.value('xpath', 'type')`/`xml.query('xpath')`/`xml.exist('xpath')`, а также rowset `.nodes` через `xml_nodes` как источник `CrossApply`/`OuterApply`);
- raw SQL для запроса целиком ([`WithSql`](xref:NextORM.Core.EntityExtensions.WithSql``1(NextORM.Core.EntityBuilder{``0},System.String)) / [`PrepareFromSql`](xref:NextORM.Core.EntityExtensions.PrepareFromSql``1(NextORM.Core.EntityBuilder{``0},System.String))) и как композируемый источник `FROM` ([`FromSql`](xref:NextORM.Core.DataContextExtensions.FromSql(NextORM.Core.IDataContext,System.String,System.Object)), рендерится как производная таблица), а также пути переиспользования неявного кэша планов / [`Prepare`](xref:NextORM.Core.EntityBuilderExtensions.Prepare``1(NextORM.Core.EntityBuilder{``0},System.Boolean,System.Threading.CancellationToken)).

## См. также

- [Хинты запросов](../guide/17-query-hints.md)
- [Provider overview](../providers/overview.md)
- [API reference](api-reference.md)

---

Source: `docs/specs/roadmap/sql-capabilities-gap-analysis.md` (sections 1–4), implementation status table at the top of
that document.
