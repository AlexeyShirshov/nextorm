# TODO: ClickHouse — доступ к колонкам без свойства сущности

> Остаток ClickHouse. Источник: `docs/specs/roadmap/sql-capabilities-gap-analysis.md` §4 п.10.
> **Статус: заблокировано** отсутствием способа сослаться на типизированную колонку по имени.

## Проблема

`IHit` описывает только 6 колонок `hits_v1`; `RefererDomain`, `IsMobile`, `IsNotBounce` и другие
доступны только через `WithSql`. Нужен способ ссылаться на типизированную колонку по имени (либо
расширить `IHit`).

## Что нужно

- API «колонка по имени» с типом (например, `EntityBuilder`-метод или расширение `IHit`).
- Резолвинг в `MemberTranslator`/проекции; учёт в план-ключе.
- SQL-gen + интеграционные тесты; docs EN+RU.

## Источник

- `IHit` (6 колонок `hits_v1`).
