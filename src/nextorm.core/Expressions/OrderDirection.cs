namespace NextORM.Core;

/// <summary>The direction of a single <c>ORDER BY</c> key.</summary>
public enum OrderDirection
{
    /// <summary>Ascending order.</summary>
    Asc,
    /// <summary>Descending order.</summary>
    Desc
}